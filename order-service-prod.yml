spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_prod
    username: root
    password: Deens@1234
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

global:
  message: "Welcome to order PROD environment!"
